extends ../layout

block content
    div(class='col-sm-8')
    
        h1= title

        ul
            each item in paymentgroup_list
                li 
                    a(href=item.url) #{item.name} 

            else
                li There are no genres. 